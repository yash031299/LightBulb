# Test Configuration
spring.profiles.active=test

# AWS Configuration for Tests
aws.region=us-east-1

# DynamoDB Local Configuration
aws.dynamodb.endpoint=http://localhost:8000
aws.dynamodb.region=us-east-1

# Table name for LightBulb
lightbulb.table.name=LightBulbTest

# Logging Configuration for Tests
logging.level.root=WARN
logging.level.com.example=DEBUG
logging.level.org.springframework=WARN
logging.level.software.amazon.awssdk=WARN

# Disable AWS credentials provider chain for tests
aws.credentials.type=static
aws.credentials.access-key-id=test-key
aws.credentials.secret-access-key=test-secret
